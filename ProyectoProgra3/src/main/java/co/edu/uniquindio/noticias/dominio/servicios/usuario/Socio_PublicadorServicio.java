package co.edu.uniquindio.noticias.dominio.servicios.usuario;

import co.edu.uniquindio.noticias.dominio.model.Publicador;
import co.edu.uniquindio.noticias.dominio.model.Contenido;

public class Socio_PublicadorServicio  {

    /**
    public void subirContenido(String nombreArchivo) {
        archivosSubidos.add(nombreArchivo);
        System.out.println("Contenido '" + nombreArchivo + "' subido por el publicador '" + getNombre() + "'.");
    }
    public void editarContenido(String nombreArchivo, Contenido nuevoContenido) {
        if (archivosSubidos.contains(nombreArchivo)) {
            setContenido(nuevoContenido);
            System.out.println("Contenido '" + nombreArchivo + "' editado por el publicador '" + getNombre() + "'.");
        } else {
            System.out.println("El archivo '" + nombreArchivo + "' no ha sido subido por el publicador '" + getNombre() + "'.");
        }
    }*/
}